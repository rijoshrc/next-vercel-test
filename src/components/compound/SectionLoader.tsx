type Props = {};

const SectionLoader = (props: Props) => {
  return (
    <section
      className="loading"
      style={{ height: "100px", width: "100%" }}
    ></section>
  );
};

export default SectionLoader;
